<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Modern Register Form</title>
  <style>
    @import url('https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap');
    body {
      background: linear-gradient(135deg, #00b4d8, #48cae4, #90e0ef, #caf0f8);
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Roboto', sans-serif;
    }
    .register-container {
      background: #fff;
      padding: 2.5rem 2rem 2rem 2rem;
      border-radius: 1.2rem;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.15);
      width: 100%;
      max-width: 380px;
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }
    .register-container h2 {
      margin: 0 0 0.5rem 0;
      font-weight: 700;
      color: #00b4d8;
      letter-spacing: 1px;
      text-align: center;
    }
    .input-group {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }
    .input-group label {
      font-size: 1rem;
      color: #333;
      font-weight: 500;
    }
    .input-group input {
      padding: 0.7rem 1rem;
      border-radius: 0.7rem;
      border: 1px solid #90e0ef;
      outline: none;
      font-size: 1rem;
      transition: border 0.2s;
    }
    .input-group input:focus {
      border: 1.5px solid #00b4d8;
    }
    .register-btn {
      background: linear-gradient(90deg, #00b4d8 60%, #48cae4 100%);
      color: #fff;
      font-weight: 700;
      border: none;
      padding: 0.9rem;
      border-radius: 0.7rem;
      font-size: 1.1rem;
      cursor: pointer;
      transition: background 0.2s, transform 0.2s;
      margin-top: 0.5rem;
    }
    .register-btn:hover {
      background: linear-gradient(90deg, #0096c7 60%, #00b4d8 100%);
      transform: translateY(-2px) scale(1.01);
    }
    .login-link {
      text-align: center;
      font-size: 0.97rem;
      margin-top: -0.2rem;
    }
    .login-link a {
      color: #00b4d8;
      text-decoration: none;
      transition: text-decoration 0.2s;
    }
    .login-link a:hover {
      text-decoration: underline;
    }
    .divider {
      display: flex;
      align-items: center;
      text-align: center;
      color: #888;
      font-size: 0.95rem;
      margin: 0.5rem 0;
    }
    .divider::before, .divider::after {
      content: '';
      flex: 1;
      border-bottom: 1px solid #e0e0e0;
      margin: 0 0.5em;
    }
    .social-register {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-top: -0.5rem;
    }
    .social-btn {
      border: none;
      border-radius: 50%;
      width: 44px;
      height: 44px;
      background: #f3f3f3;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 1.3rem;
      transition: background 0.2s;
    }
    .social-btn:hover {
      background: #caf0f8;
    }
    @media (max-width: 400px) {
      .register-container { padding: 1.5rem 0.5rem; }
    }
  </style>
  <!-- Font Awesome for social icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"/>
</head>
<body>
  <form  action="/register" method="post" class="register-container" autocomplete="on">
    <h2>Create Account</h2>
    <div class="input-group">
      <label for="fullname">Full Name</label>
      <input id="username" name="username" type="text" required placeholder="Enter your Username" autocomplete="name" />
    </div>
    <div class="input-group">
      <label for="email">Email</label>
      <input id="email" name="email" type="email" required placeholder="Enter your email" autocomplete="username"/>
    </div>
    <div class="input-group">
      <label for="password">Password</label>
      <input id="password" name="password" type="password" required placeholder="Create a password" autocomplete="new-password" />
    </div>
    <div class="input-group">
      <label for="confirm-password">Confirm Password</label>
      <input id="confirm-password" name="confirm-password" type="password" required placeholder="Confirm your password" autocomplete="new-password" />
    </div>
    <button class="register-btn" type="submit">Register</button>
    <div class="login-link">
      Already have an account? <a href="/signin">Login</a>
    </div>
    <div class="divider">or sign up with</div>
    <div class="social-register">
      <button type="button" class="social-btn" title="Sign up with Google">
        <i class="fab fa-google"></i>
      </button>
      <button type="button" class="social-btn" title="Sign up with Facebook">
        <i class="fab fa-facebook-f"></i>
      </button>
      <button type="button" class="social-btn" title="Sign up with Twitter">
        <i class="fab fa-twitter"></i>
      </button>
    </div>
  </form>
</body>
</html>